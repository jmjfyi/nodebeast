const router = require('./router');

const handlers = {};

module.exports = {
	//adds a handler to the 
	'add': (path, method) => {
		handlers[name] = { 'run': method };
	},

	'run': (name, callback) => {
		
	}

};